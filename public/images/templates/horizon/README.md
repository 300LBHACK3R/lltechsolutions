# Landscape Contracting screenshots

These are the three supplied captures of the independent Horizon Contracting Group redesign concept. They are design references, not evidence of a client relationship. A customer's version uses their own approved brand, imagery and content.

The screenshot order, captions, alternative text and dimensions are configured in `src/data/horizon-demo.json`:

1. `hero.png` — 2048 × 1082
2. `services.png` — 2048 × 978
3. `gallery.png` — 2048 × 967

Replace captures here when cleaner screenshots are available. Keep filenames or update the JSON; supply the actual pixel dimensions and meaningful descriptions. PNG, WebP and JPEG are supported. The catalogue cover is configured separately in `src/data/website-collection.ts`; update its dimensions if replacing the hero with a different size.

The demo `url` remains null. Set it only to the actual public HTTPS demo after verifying it opens without a login. The detail page shows the screenshot gallery without advertising an unavailable live demo. This catalogue update does not include the separate Horizon demo's source or publish a new demo.
